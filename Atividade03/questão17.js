let a = 5
let b = 9
let c = 2
let triangulo

triangulo = (a*a)+(b*b)

if(triangulo==c){
    console.log(`Os valores "${a}", "${b}" e "${c}" formam um triângulo retângulo`); 
}
else{
    console.log(`Os valores "${a}", "${b}" e "${c}" não formam um triângulo retângulo`); 
}
